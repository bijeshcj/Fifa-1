package com.prokarma.fifa.webservice;
/*package com.pk.restrequesttest.webservice;

import android.content.Context;

import com.calldropdetect.MLog;
import com.calldropdetect.User;
import com.calldropdetect.localdb.LocalDBStorage;

public class SignalDetail {

	public void sendSignalDetail(Context context,User user) throws Exception{
		try{
			String userId = new LocalDBStorage(context).checkIsNewUser(context, user);
			if(userId != null){
				user.setUserId(userId);
				new RequestManager().sendSignalDetail(context,user);
			}
		}catch(Exception e){
			MLog.e("","Exception In the Registration");
			e.printStackTrace();
			throw e;
		}
	}
	
}
*/